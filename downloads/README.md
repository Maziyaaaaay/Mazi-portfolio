# Downloads slot

Drop `mazin-kp-onepager.pdf` here, then flip
`WORK_WITH_ME.onePager.available` to `true` in
`src/lib/constants.ts` — the download button in the contact section
appears automatically.
